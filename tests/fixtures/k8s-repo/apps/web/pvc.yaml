apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: web-data, namespace: prod}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: managed-premium
  resources: {requests: {storage: 20Gi}}
